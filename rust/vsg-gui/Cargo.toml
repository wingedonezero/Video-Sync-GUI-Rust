[package]
name = "vsg-gui"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
time = "0.3"

# GUI stack
imgui = "0.12"
imgui-winit-support = "0.12"
glow = "0.13"
winit = "0.30"

# Shared core
vsg-core = { path = "../vsg-core" }
