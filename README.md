# Video-Sync-GUI-Rust

Rust reimplementation of the Video Sync GUI tool.

- `vsg-core`: library containing core logic, process handling, mkvmerge options builder, analysis, and planning.
- `vsg-cli`: headless CLI for analyze/merge workflows.
- `vsg-gui`: imgui frontend (placeholder).
