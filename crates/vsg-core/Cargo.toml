[package]
name = "vsg-core"
version = "0.1.0"
edition = "2021"

[dependencies]
# Keep ranges broad so `cargo update` pulls the latest compatible releases.
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
regex = "1"
