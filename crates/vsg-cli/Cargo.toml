
[package]
name = "vsg-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
tracing = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
vsg-core = { path = "../vsg-core" }
