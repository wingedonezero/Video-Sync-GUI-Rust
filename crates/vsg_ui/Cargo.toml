[package]
name = "vsg_ui"
description = "Video Sync GUI - UI placeholder (awaiting CXX-Qt implementation)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
# Core library
vsg_core = { path = "../vsg_core" }

# Note: UI implementation will use CXX-Qt (not yet added)
# This crate is currently a placeholder
