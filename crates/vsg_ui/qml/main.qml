// main.qml — Entry point that loads the main window.
// 1:1 port of Python main.py which creates QApplication + MainWindow.

import QtQuick 2.15
import QtQuick.Window 2.15
import com.vsg.ui 1.0

MainWindow {
    visible: true
}
