# Spec
Fixtures/goldens for spec-first development.

- `fixtures/`: tiny example probes
- `goldens/`: expected outputs
- `README.md`: step-by-step rules and file shapes

Step 1 scope: Extraction selection + manifests (mkvextract runner, binary-colocated dirs).
